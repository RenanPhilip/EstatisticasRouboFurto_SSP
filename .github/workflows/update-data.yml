name: Atualizar Dados SSP-SP

on:
  push:
    branches:
      - main
  workflow_dispatch:
  schedule:
    - cron: '0 3 1 * *'

jobs:
  update-data:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    timeout-minutes: 120 # Aumentado para 2 horas

    steps:
      - name: Checkout repositório
        uses: actions/checkout@v4

      - name: Verificar e preparar scripts/
        run: |
          mkdir -p scripts
          cd scripts
          if [ ! -f package-lock.json ]; then
            echo "package-lock.json não encontrado. Criando..."
            npm init -y
            npm install xlsx axios --save
          fi

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
          cache-dependency-path: scripts/package-lock.json

      - name: Criar pasta data se não existir
        run: mkdir -p data

      - name: Instalar dependências em scripts/
        run: |
          cd scripts
          npm ci

      - name: Baixar e processar dados da SSP (com logs)
        run: |
          cd scripts
          echo "Iniciando processamento às $(date)"
          node --max-old-space-size=8192 update-data.js
          echo "Processamento concluído às $(date)"
        env:
          NODE_ENV: production

      - name: Verificar JSONs gerados (Debug)
        run: |
          ls -la data/
          for file in data/*_*.json; do
            echo "Conteúdo de $file:"
            head -n 20 "$file" || echo "Arquivo vazio ou inexistente!"
          done

      - name: Commit e push das mudanças
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add data/*.json
          if git diff --staged --quiet; then
            echo "Nenhuma mudança nos dados. Pulando commit."
          else
            git commit -m "🤖 Atualização automática de dados SSP - $(date +'%d/%m/%Y %H:%M')"
            git push
          fi