name: Atualizar Dados SSP-SP

on:
  # ATIVADOR TEMPORÁRIO PARA TESTES: Roda em push (COMENTE APÓS TESTAR!)
  push:
    branches:
      - main

  # ATIVADOR MANUAL
  workflow_dispatch:

  # ATIVADOR AGENDADO (mensal, dia 1 às 3h UTC ~ 0h BRT)
  schedule:
    - cron: '0 3 1 * *'

jobs:
  update-data:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repositório
        uses: actions/checkout@v4  # Atualizado para v4

      - name: Setup Node.js
        uses: actions/setup-node@v4  # Atualizado para v4
        with:
          node-version: '18'
          cache: 'npm'  # Cache para dependências

      - name: Criar pasta scripts se não existir
        run: |
          mkdir -p scripts
          mkdir -p data  # Garante que data/ existe

      - name: Inicializar package.json em scripts/
        run: |
          cd scripts
          if [ ! -f package.json ]; then
            npm init -y
            npm install xlsx axios  # xlsx para Excel, axios para download
          fi

      - name: Baixar e processar dados da SSP (com logs)
        run: |
          cd scripts
          node --max-old-space-size=4096 update-data.js
        env:
          NODE_ENV: production

      - name: Verificar JSONs gerados (Debug)
        run: |
          ls -la data/
          echo "Conteúdo de estatisticas.json:"
          head -n 20 data/estatisticas.json || echo "Arquivo vazio!"
          echo "Conteúdo de mapa-ocorrencias.json:"
          head -n 20 data/mapa-ocorrencias.json || echo "Arquivo vazio!"

      - name: Commit e push das mudanças
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add data/*.json
          if git diff --staged --quiet; then
            echo "Nenhuma mudança nos dados. Pulando commit."
          else
            git commit -m "🤖 Atualização automática de dados SSP - $(date +'%d/%m/%Y %H:%M')"
            git push
          fi

  # OPCIONAL: Trigger deploy após update (descomente para automação completa)
  # deploy-trigger:
  #   needs: update-data  # Depende do job acima
  #   runs-on: ubuntu-latest
  #   if: github.event_name == 'schedule' || github.event_name == 'workflow_dispatch'
  #   steps:
  #     - name: Trigger Deploy Workflow
  #       uses: actions/github-script@v7
  #       with:
  #         script: |
  #           github.rest.actions.createWorkflowDispatch({
  #             owner: context.repo.owner,
  #             repo: context.repo.repo,
  #             workflow_id: 'deploy.yml',
  #             ref: 'main'
  #           })