name: Atualizar Dados SSP-SP

on:
  # ATIVADOR TEMPORÁRIO PARA TESTES (comente após testar)
  push:
    branches:
      - main

  # ATIVADOR MANUAL
  workflow_dispatch:

  # ATIVADOR AGENDADO (mensal, dia 1 às 3h UTC ~ 0h BRT)
  schedule:
    - cron: '0 3 1 * *'

jobs:
  update-data:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repositório
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
          cache-dependency-path: scripts/package-lock.json  # Aponta para o lock em scripts/

      - name: Criar pasta scripts e data se não existirem
        run: |
          mkdir -p scripts
          mkdir -p data

      - name: Inicializar package.json e instalar dependências em scripts/
        run: |
          cd scripts
          if [ ! -f package.json ]; then
            npm init -y
            npm install xlsx axios --save  # Gera package-lock.json
          fi
          npm ci  # Usa package-lock.json para instalação consistente

      - name: Baixar e processar dados da SSP (com logs)
        run: |
          cd scripts
          node --max-old-space-size=4096 update-data.js
        env:
          NODE_ENV: production

      - name: Verificar JSONs gerados (Debug)
        run: |
          ls -la data/
          echo "Conteúdo de estatisticas.json:"
          head -n 20 data/estatisticas.json || echo "Arquivo vazio!"
          echo "Conteúdo de mapa-ocorrencias.json:"
          head -n 20 data/mapa-ocorrencias.json || echo "Arquivo vazio!"

      - name: Commit e push das mudanças
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add data/*.json
          if git diff --staged --quiet; then
            echo "Nenhuma mudança nos dados. Pulando commit."
          else
            git commit -m "🤖 Atualização automática de dados SSP - $(date +'%d/%m/%Y %H:%M')"
            git push
          fi