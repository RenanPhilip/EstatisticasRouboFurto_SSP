name: Atualizar Dados SSP-SP

on:
  # ATIVADOR TEMPORÁRIO PARA TESTES (comente após testar)
  push:
    branches:
      - main

  # ATIVADOR MANUAL
  workflow_dispatch:

  # ATIVADOR AGENDADO (mensal, dia 1 às 3h UTC ~ 0h BRT)
  schedule:
    - cron: '0 3 1 * *'

jobs:
  update-data:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repositório
        uses: actions/checkout@v4

      - name: Verificar e preparar scripts/
        run: |
          mkdir -p scripts
          cd scripts
          if [ ! -f package-lock.json ]; then
            echo "package-lock.json não encontrado. Criando..."
            npm init -y
            npm install xlsx axios --save
          fi

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
          cache-dependency-path: scripts/package-lock.json  # Deve funcionar agora

      - name: Criar pasta data se não existir
        run: mkdir -p data

      - name: Instalar dependências em scripts/ (se necessário)
        run: |
          cd scripts
          npm ci  # Usa package-lock.json existente

      - name: Baixar e processar dados da SSP (com logs)
        run: |
          cd scripts
          node --max-old-space-size=4096 update-data.js
        env:
          NODE_ENV: production

      - name: Verificar JSONs gerados (Debug)
        run: |
          ls -la data/
          echo "Conteúdo de estatisticas.json:"
          head -n 20 data/estatisticas.json || echo "Arquivo vazio!"
          echo "Conteúdo de mapa-ocorrencias.json:"
          head -n 20 data/mapa-ocorrencias.json || echo "Arquivo vazio!"

      - name: Commit e push das mudanças
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add data/*.json
          if git diff --staged --quiet; then
            echo "Nenhuma mudança nos dados. Pulando commit."
          else
            git commit -m "🤖 Atualização automática de dados SSP - $(date +'%d/%m/%Y %H:%M')"
            git push
          fi